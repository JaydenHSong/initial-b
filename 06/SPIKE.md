# S06 스파이크 기록

## 1. Anthropic SDK 직접 + 구조화 출력 — **됐다** (2026-09-08)

**판정 질문:** Vercel 함수에서 Anthropic SDK로 Claude를 부르면 JSON 스키마대로 5줄이
오나? `usage`로 건당 비용이 나오나? 키는 브라우저에 안 가나?

**방법:** `client.messages.parse({ output_config: { format: zodOutputFormat(스키마) } })` —
스키마 = `positives ≤2 · pain_points ≤2 · verdict 1` (합쳐서 5줄). 케이스 리뷰 10개를
POST로 넣었다. 모델 `claude-opus-5`, 키는 `ANTHROPIC_API_KEY`(Vercel env)만 — 브라우저는
`/api/...`만 부른다.

| 관측 | 값 |
|---|---|
| `parsed_output` | 스키마 그대로 5줄 · 한국어 · 리뷰에 근거(뻑뻑한 버튼·황변·먼지·탈착 / MagSafe·카메라 링) |
| `stop_reason` | `end_turn` (thinking_tokens 0) |
| `usage` | 입력 1,237 · 출력 355 토큰 |
| 건당 비용 | Opus 5 **$0.0151** · Sonnet 5 $0.0060 · Haiku 4.5 $0.0030 |
| **1천 건이면** | **Opus 5 $15.06** · Sonnet 5 $6.02 · Haiku 4.5 $3.01 |
| 응답 시간 | 11.3초 (Opus 5, 기본 effort) |

**소넷 재측정 (같은 리뷰 10개, 같은 스키마):** 결과 5줄의 내용이 Opus와 사실상 동일
(MagSafe·마감 / 버튼·탈착·변색·먼지) · 출력 259토큰 · **5.6초** · **1천 건 $5.06**.
→ **모델은 `claude-sonnet-5`로 확정 (Jayden 결정).** Opus 5 대비 품질 손실 없이 시간 절반, 비용 1/3.

**셋업 시간:** 약 10분 (SDK 설치 → 함수 작성 → 배포 → 첫 JSON). 실제로는 키 등록 대기가
더 길었다 — 로컬에 `ant` CLI도 `ANTHROPIC_API_KEY`도 없어 서버리스에서만 판정 가능.

**막힌 지점 1개:** 없음 — 첫 호출에 통과. 다만 **11초**는 UX 관측값으로 남긴다: 수요일에
`output_config.effort: "low"`로 시간·비용을 재본다(요약 작업이라 낮은 effort가 맞을 가능성).

**문서 품질:** 상 — `parse` + `zodOutputFormat` 예제 그대로 됐고, zod 4와도 문제 없음.

**무료 한도:** 해당 없음(종량제). 데모 멘트의 근거는 위 표 — 추정이 아니라 `usage` 실측.

**정리 필요:** `api/spike.js`는 수요일 구현이 대체하면 제거한다.

## 2. `/dp/` 원본 HTML에 리뷰가 있나 — **안 됐다** (2026-09-08)

**판정 질문:** 우리가 이미 받는 아마존 상품 페이지(`/dp/ASIN`) 원본 HTML에 리뷰 본문이
몇 개 들어 있나? 5개 이상이면 「보드에서 가져오기」를 붙이고, 아니면 붙여넣기만.

**방법:** `initial-b06` 서버리스에서 직접 fetch → `data-hook="review-body"` 정규식 카운트.
버릴 코드(`api/spike.js`, mode=reviews/find).

| 시도 | 결과 |
|---|---|
| 1~2회 (두 ASIN) | 4KB 봇 페이지 — 새 프로젝트 IP를 첫 요청부터 차단 (S04 패턴) |
| 재시도 40초 간격 | 2회차에 진짜 페이지 1253KB 수신, **리뷰 본문 0개** |
| `find` 모드로 마커 전수 조사 (1009KB 정상 페이지) | `review-body` 0 · `review-text-content` 0 · `customer_review-` 0 · `reviewsMedley` 0 · "Top reviews" 0 |

**결론: 정규식 문제가 아니라 데이터 부재다.** 아마존은 리뷰 섹션을 서버 렌더에서 빼고
뒤늦게 하이드레이션한다 — S03 Yelp에서 적은 "렌더된 화면과 서버가 받는 것은 다르다"의
세 번째 재현. 리뷰 전용 페이지(`/product-reviews/`)는 로그인 벽이라 기획서의
「안 만드는 것 1번」 그대로 손대지 않는다. **「보드에서 가져오기」는 안 만든다. 입력은
붙여넣기 하나.** 골이 붙여넣기라 완료 판정은 흔들리지 않는다. `BRD_WS`도 이번 주엔 불필요.

**셋업 시간:** 약 12분 (06/ 생성 → initial-b06 → 배포 → 첫 판정. 봇 페이지 대기 포함)

**막힌 지점 1개:** 새 Vercel 프로젝트의 첫 요청들이 전부 봇 페이지 — 판정 대상이 "정규식이
맞나"였는데 실제로는 "진짜 페이지를 받았나"부터 갈라야 했다. `bot` 관측값을 응답에 같이
찍어둔 덕에 두 질문이 섞이지 않았다.

**문서 품질:** 해당 없음 (HTML 파싱) **무료 한도:** 비용 0

## 3. 렌더된 DOM(Bright Data 브라우저)에는 리뷰가 있나 — **안 됐다** (2026-09-09, 3회 후 중단)

**판정 질문:** 원본 HTML엔 없지만, 실제 브라우저로 렌더하고 스크롤하면 리뷰 본문이 나오나?
(Jayden: "URL을 넣으면 리뷰는 시스템이 가져와야 하는 것 아닌가" — 맞는 말이라 마지막 길을 봤다)

| 시도 | 결과 |
|---|---|
| 렌더 후 20초 대기 ×2 | DOM 1.9MB, `review-body` **0개** |
| + 스크롤 6회(리뷰 섹션 뷰포트 진입) | goto 30초 타임아웃 (BRD 변동) |
| 같은 것 재실행 | 페이지 높이 14,482px·DOM 2.4MB·`#reviewsMedley` 컨테이너 **있음**·별점 훅 3개 잡힘 — 그런데 **`review-body` 0개**, 총 52초 |

**결론:** 아마존은 리뷰 섹션의 껍데기(컨테이너·별점)까지는 렌더하지만 **리뷰 본문은
이 세션(데이터센터/프록시)에 주지 않는다** — 로그인 세션에 묶였거나 별도 인증 XHR로
추정. 설령 나와도 건당 30~52초라 "URL 넣으면 바로"의 UX가 안 된다(S03 Yelp 판정과 같은 결론).
자동 수집의 남은 길은 유료 리뷰 데이터 API(Apify 아마존 리뷰 액터 등)뿐 — 계정·비용·
별도 스파이크가 필요하고 수요일 오후라 이번 주 범위 밖으로 보고했다.

**막힌 지점 1개 (덤, 보안):** `BRD_WS` 값에 다음 변수 이름이 딸려 들어가 URL 파싱이 깨졌고,
스파이크가 `e.message`를 그대로 응답에 실어 **시크릿이 공개 응답으로 한 번 노출**됐다.
에러는 이름만 돌려주도록 고쳤고 존 비밀번호 로테이션을 권고했다. 교훈: 에러 메시지에
URL이 섞일 수 있는 코드는 메시지를 밖으로 내보내지 않는다.
