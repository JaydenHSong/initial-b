.DS_Store
node_modules/
.env*
*.log
.vercel

# 세션별 격리 작업 공간 — 레포 사본이 통째로 들어앉는다. 커밋되면 안 된다.
# 지금까지 .git/info/exclude로만 막고 있었는데 그건 커밋되지 않아 clone에 따라오지 않는다.
.claude/worktrees/

# 루트의 package.json은 Vercel CLI 로컬 설치 흔적일 뿐이다.
# 주차 폴더(01/, 02/ …)의 package.json은 실제 프로젝트 파일이므로 무시하지 않는다.
/package.json
/package-lock.json

# 서비스 계정 키 — 공개 리포다. 다운로드 폴더에서 여기로 옮겨두는 사고가 잦다.
# 키는 레포 밖(~/.config/ibd/)에 두고 경로로만 참조한다.
/*.json
*service-account*.json
*adminsdk*.json

# 팀 내부 문서 — 공개 리포에 올리지 않는다 (로컬에는 그대로 있음)
# (훈련 가이드는 01/guide.html, 규칙 파일은 01/docs/CLAUDE.md로 공개 전환됨)
/CLAUDE.md
iBD_바이브코딩_레지스트리.xlsx
